import java.util.*;
public class Try {
    public static void main(String[] args) {
        List<Integer> ints = new ArrayList<>();
        ints.add(3);
        ints.add(4);
        for(int i: ints){
            System.out.println(i*2);
        }
    }
}
